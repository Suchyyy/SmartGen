<UserControl x:Class="SmartGen.UserControls.LayerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SmartGen.UserControls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" x:Name="This"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="110" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="{Binding ElementName=This, Path=LayerNo, StringFormat='Hidden layer {0}'}"
                   VerticalAlignment="Center" />
        <metro:NumericUpDown Minimum="1" Maximum="2000" Value="{Binding ElementName=This, Path=Size}" Margin="10"
                             Style="{StaticResource MaterialDesignNumericUpDown}" Grid.Column="1" Width="80"
                             TextAlignment="Center" />
    </Grid>
</UserControl>